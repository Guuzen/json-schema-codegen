{
    "description": "A collection of tags",
    "type": "object",
    "required": ["tags", "tag"],
    "properties": {
        "tags": {
            "type": "array",
            "items": {
                "enum": ["featured", "sale", "new", "trending"],
                "description": "Available tag types"
            },
            "description": "List of tag enums"
        },
        "tag": {
            "anyOf": [
                { "type": "null" },
                {
                    "enum": ["featured", "sale", "new", "trending"],
                    "description": "Tag"
                }
            ],
            "description": "List of tag enums or null"
        }
    }
}
