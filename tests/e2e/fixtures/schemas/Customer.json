{
    "type": "object",
    "required": ["homeAddress", "billingAddress"],
    "properties": {
        "homeAddress":    {"$ref": "address/Address.json"},
        "billingAddress": {"$ref": "billing/Address.json"}
    }
}
